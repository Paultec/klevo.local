<section id="accordion">

    <?php echo $this->showList($this->categoryList, $this->seoUrlParams, $this->route); ?>

    <hr class="delim">

    <div class="accordion">
        <h3 class="filled">Производители</h3>
        <div>
            <div id="form-filter"></div>
            <?php
            $seoUrlParams = $this->seoUrlParams;
            $flag         = $this->sessionFlag;

            if (isset($flag['catalog']) && !isset($flag['brand'])) {
                $seoUrlParams['param2'] = $seoUrlParams['param1'];
            }

            foreach ($this->brandList as $brand) {
                $seoUrlParams['param1'] = $brand['translit'];
                echo '<span class="filter-item"><a href="' .
                    $this->url($this->route, $seoUrlParams)
                    . '" class="empty catalog-link">' . $brand['name'] . '</a></span>' . "\n";
            }
            ?>
        </div>
    </div>
</section>